"use client";

export default function AuthenticatedIndex() {
  // Placeholder client page to satisfy Next.js client reference manifest
  // We immediately no-op; navigation is handled elsewhere (root page redirects to /chat)
  return null;
}
